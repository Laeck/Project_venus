{% extends 'base.html.twig' %}

{% block body %}

        {{ dump(oeuvres) }}

        {# Partie galerie #}
<div class="album py-5">
	<div class="container">
		{% for oeuvres in oeuvres %}
		<div class="col-md-6">
				<div class="card mb-4 shadow-sm">
					<div class="card-body">
					<h4> {{ oeuvres.nom }} </h4>
					<p class="card-text"> {{ oeuvres.description }} </p>
					<p class="card-text"> Oeuvre crée le : {{ oeuvres.datecreation | date('d/m/Y') }} </p>
				</div>
			</div>
		{% endfor %}
		</div>
	</div>
</div>

{% endblock %}

